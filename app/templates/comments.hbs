<form>
	<div class="form-group">
		<label for="bills">Comments</label>
		{{input value=value type="text" class="form-control" name="comments"}}
	</div>
	<button type="submit" class="btn btn-default" {{action 'getData'}}>Submit</button>
</form>
{{high-charts chartOptions=chartOptions mode='Map' content=chartData}}